// Stub for the @claimit/mongodb-types workspace package. Every export
// is a wildcard type — these are *type-only* imports throughout
// apps/web/, so the values don't matter; the file just has to resolve.
// biome-ignore lint/suspicious/noExplicitAny: type-only shim for workspace types
type Any = any;

export type Platform = string;
export type Category = string;
export type ClaimType = string;
export type ClaimOutcome = string;
export type PurchaseStatus = string;
export type NotificationEventType = string;
export type NotificationEvent = Any;
export type User = Any;
export type SendMode = string;
