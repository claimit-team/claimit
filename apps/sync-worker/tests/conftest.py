"""Shared pytest configuration for the sync-worker test suite.

`asyncio_mode = "auto"` lives in `pyproject.toml [tool.pytest.ini_options]`.
This file exists so the directory is a discoverable conftest root for any
future shared fixtures.
"""
